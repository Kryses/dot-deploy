---
- name: Install nvm
  git: 
    repo: "https://github.com/creationix/nvm.git"
    dest: "~/.nvm"
  tags: nvm

- name: Install node and set version
  shell: "source ~/.nvm/nvm.sh && nvm install 8.11.3" 
  args:
    executable: /bin/bash
